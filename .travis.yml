sudo: false
dist: xenial

services:
  - mongodb
  - redis
  - mysql
os:
  - linux
language: python

python:
  - "3.7"

install:
  - pip install tox-travis

before_script:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS risk_control;'

script:
  - tox
